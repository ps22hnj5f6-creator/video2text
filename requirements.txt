# 短视频批量转文字 - 依赖清单
# 核心依赖
faster-whisper>=1.0.0      # 语音转写引擎（比openai-whisper快4倍）
gradio>=4.0.0              # Web界面
yt-dlp>=2024.0.0           # 视频下载

# 音频处理
imageio-ffmpeg>=0.4.0      # ffmpeg二进制（无需系统安装ffmpeg）

# 导出格式
openpyxl>=3.0.0            # Excel导出（可选）

# 备用引擎（可选）
# tencentcloud-sdk-python>=3.0.0  # 腾讯云ASR（更高准确率）
